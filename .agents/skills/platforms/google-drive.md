---
description: Google Drive / Google Workspace API の統合知識。ファイル操作、Sheets/Docs/Slides API、サービスアカウント、共有ドライブ設計、Google Apps Script、権限管理を含む。
---

# Google Drive スキル

## 作業開始チェックリスト

1. OAuth 2.0 / サービスアカウント / API キーのどれを使うか確認する
2. 必要最小限の API スコープを特定する（`drive.readonly` vs `drive.file` vs `drive`）
3. マイドライブ / 共有ドライブの構成、既存のフォルダ階層を把握する
4. Drive API のクォータ制限（1日あたり / 100秒あたり）を確認する
5. 扱うファイルに機密情報・個人情報が含まれるか確認する

## コーディングルール

### API 操作
- ファイル検索には `q` パラメータでクエリを最適化する（全件取得してからフィルタしない）
- 大量ファイル操作にはバッチリクエスト API を使用する（1リクエスト最大100件）
- `fields` パラメータで必要なフィールドのみ取得する（Partial Response）
- ページネーションには `pageToken` を使い、全ページを処理する
- `supportsAllDrives=true` を共有ドライブ対応時に必ず指定する

### 権限管理
- 共有設定は最小権限の原則に従う（閲覧者 > コメント可 > 編集者 > オーナー）
- 外部共有は明示的な許可がない限り禁止する
- リンク共有（`anyone` / `anyoneWithLink`）は機密ファイルに使用しない
- 共有ドライブのメンバー管理はドライブ単位で行い、ファイル単位の権限変更を最小化する

### エラーハンドリング
- 403（権限不足）/ 404（ファイル未検出）/ 429（レート制限）を区別して処理する
- 429 エラー時は Exponential Backoff でリトライする
- ファイルの存在確認後に操作する場合、TOCTOU を考慮する
- 大容量ファイルのアップロードには Resumable Upload を使用する

### Google Apps Script
- スクリプトはプロジェクト単位で管理し、バージョン管理する（clasp 使用推奨）
- トリガー設定は明示的に文書化する（時間ベース / イベントベース）
- 実行時間制限（6分 / 30分）を考慮し、長時間処理は分割する

## セキュリティ

- サービスアカウントの秘密鍵（JSON）をリポジトリにコミットしない
- OAuth クライアント ID / シークレットは環境変数で管理する
- サービスアカウントには必要最小限のスコープのみ付与する
- Domain-wide Delegation は慎重に使用する（必要性を文書化）
- 共有リンクの有効期限を設定する（該当する場合）
- Drive Activity API で定期的にアクセスログを監査する

## テスト

- **義務**: API 操作のテストはサンドボックスフォルダ内で実施する
- **テスト種類**:
  - ユニットテスト: API レスポンスのモックを使って処理ロジックを検証する
  - 統合テスト: テスト用フォルダで実際の API 呼び出しを検証する
  - 権限テスト: 異なる権限レベルでの操作可否を確認する
  - クリーンアップ: テスト後にテスト用ファイル/フォルダを自動削除する
- **完了前必須**: テスト用ファイルが全て削除され、本番フォルダに影響がないことを確認する

## 完了条件

- [ ] API 操作が正常に動作している（CRUD、検索、共有）
- [ ] エラーハンドリング（403, 404, 429）が実装されている
- [ ] 認証情報がソースコードにハードコードされていない
- [ ] 必要最小限の API スコープのみ使用している
- [ ] 大容量ファイル対応（Resumable Upload）が必要な場合、実装されている
- [ ] テスト用データのクリーンアップが完了している

## 連携先エージェント

- **COO**: ワークフロー自動化の要件定義、運用ルールの策定
- **data-engineer**: Drive → データパイプラインの連携（CSV/JSON エクスポート等）
- **legal-compliance-checker**: ファイル共有の法的要件・情報セキュリティポリシー確認
- **secretary**: 議事録・ドキュメント管理のワークフロー構築
