# インフラストラクチャ設計ルール

## 第一原則: オブザーバビリティ・ファースト

インフラ構成の選定時、「障害発生時にデバッグできるか」を最優先基準とする。
高機能・低コストよりも、ログ・メトリクス・トレースが取れる構成を選ぶ。

## エラーデバッグ基盤

### 構造化ログの必須化
- 全サービス/インスタンスでJSON構造化ログを出力する
- 最低限のフィールド: `timestamp`, `level`, `service`, `requestId`, `message`, `error`
- 自由文のログ（`echo "something happened"`）は禁止

```json
{
  "timestamp": "2026-03-25T10:30:00Z",
  "level": "error",
  "service": "api-server",
  "requestId": "req-abc123",
  "message": "Failed to query user",
  "error": {
    "type": "ConnectionTimeout",
    "message": "Connection to db-primary timed out after 5000ms",
    "stack": "..."
  },
  "context": {
    "userId": "user-456",
    "dbHost": "db-primary.internal",
    "queryDurationMs": 5000
  }
}
```

### リクエストIDの伝播
- リクエストの入口で一意のIDを採番し、全ての下流サービス・DBクエリ・外部API呼び出しに伝播する
- ロードバランサー → アプリサーバー → DB → 外部API の全区間でIDが追跡可能な状態にする
- ヘッダー名の標準: `X-Request-Id` または `traceparent`（W3C Trace Context）

### エラーの分類とアラート
- エラーを3段階に分類し、アラートレベルを分ける:
  - **Fatal**: サービス停止レベル → 即時通知（PagerDuty/Slack/電話）
  - **Error**: 機能障害だがサービスは継続 → 5分以内に通知
  - **Warning**: 性能劣化・閾値接近 → 日次サマリーで通知
- 「全エラーを同じ重要度で通知」はアラート疲れを招く。分類を必ず設計する

### DB エラーログ
- スロークエリログを有効化する（閾値: 100ms 以上）
- デッドロック検出ログを有効化する
- 接続プールの枯渇をログ出力する（available/waiting/max の3値）

## パフォーマンスデバッグ基盤

### メトリクス収集の必須化
- 全サービスで以下の RED メトリクスを収集する:
  - **Rate**: リクエスト数/秒
  - **Errors**: エラー率（%）
  - **Duration**: レイテンシ（p50, p95, p99）
- DB/キャッシュには USE メトリクスを追加:
  - **Utilization**: CPU/メモリ/ディスク使用率
  - **Saturation**: キュー長、待ち行列数
  - **Errors**: 接続エラー数

### DB パフォーマンス
- クエリ実行計画（EXPLAIN）をログに記録可能にする（本番は閾値超過時のみ）
- インデックス使用状況を定期監視する（未使用インデックスの検出）
- コネクションプールの使用状況をメトリクスとして公開する
  - `pool_size`, `pool_active`, `pool_idle`, `pool_waiting`

### インスタンスパフォーマンス
- CPU / メモリ / ディスクI/O / ネットワークI/O のメトリクスを収集する
- オートスケーリングの閾値とクールダウン期間を文書化する
- スケールイベント（スケールアウト/イン）をログに記録する

### レイテンシの分解
- 全体のレスポンスタイムだけでなく、区間ごとのレイテンシを測定する:
  - ロードバランサー → アプリ（ネットワーク）
  - アプリ内処理（ビジネスロジック）
  - アプリ → DB（クエリ実行）
  - アプリ → 外部API（外部依存）
- ボトルネックの特定に「どの区間が遅いか」の情報が必須

### ベースラインの記録
- 正常時のパフォーマンスベースラインを文書化する
- 異常判定には「通常と比べてどうか」が必要。ベースラインなしでは判断できない

```markdown
## パフォーマンスベースライン（2026-Q1）
| メトリクス | p50 | p95 | p99 |
|-----------|-----|-----|-----|
| API レスポンス | 45ms | 120ms | 350ms |
| DB クエリ | 5ms | 25ms | 80ms |
| 外部API | 200ms | 500ms | 1200ms |
```

## 監査デバッグ基盤

### インフラ変更の監査ログ
- インフラ構成の変更（手動・自動問わず）を全て記録する:
  - インスタンスの起動/停止/スケール
  - セキュリティグループ/ファイアウォール変更
  - DB スキーマ変更（マイグレーション）
  - 環境変数/シークレットの変更
  - デプロイ（誰が・いつ・どのバージョンを）

### DB 監査ログ
- DDL（CREATE/ALTER/DROP）を全て記録する
- 特権操作（GRANT/REVOKE、データの直接UPDATE/DELETE）を記録する
- 大量データ操作（バルクINSERT/DELETE）をアラート対象にする

```sql
-- PostgreSQL: pgaudit 有効化例
-- DDL + 特権操作 + 大量変更を記録
ALTER SYSTEM SET pgaudit.log = 'ddl,role,write';
ALTER SYSTEM SET pgaudit.log_level = 'log';
```

### アクセスログ
- 全サービスへのアクセスログを保持する（最低90日、推奨1年）
- ログに含める情報: タイムスタンプ、送信元IP、メソッド、パス、ステータスコード、レスポンスタイム、ユーザーID
- DB への接続ログ: 接続元IP、ユーザー名、接続/切断タイムスタンプ

### 変更の追跡可能性
- 「誰がいつ何を変えたか」を全レイヤーで追跡可能にする:
  - **アプリケーション**: API監査ログ（`api-sdk.md` 参照）
  - **インフラ**: IaC の Git 履歴 + Cloud Audit Log
  - **DB**: マイグレーション履歴 + pgaudit/Cloud SQL Audit Log
  - **シークレット**: Secret Manager のアクセスログ

## インフラ構成の選定基準

### 選定時のチェックリスト
新しいサービス/ミドルウェア/マネージドサービスを採用する際に確認する:

| # | 確認項目 | 不合格基準 |
|---|---------|-----------|
| 1 | 構造化ログを出力できるか | ログなし or 自由文のみ |
| 2 | メトリクスをエクスポートできるか | CPU/メモリすら取れない |
| 3 | 分散トレースに参加できるか | requestId の伝播が不可能 |
| 4 | 監査ログを有効化できるか | 変更履歴が取れない |
| 5 | アラートを設定できるか | 閾値ベースの通知ができない |
| 6 | ログの保持期間を制御できるか | ログが自動削除される or 保持設定なし |

**上記のいずれかが不合格の場合、代替サービスを検討する。**

### IaC（Infrastructure as Code）必須
- インフラ構成は必ずコード化する（Terraform, Pulumi, CloudFormation等）
- 手動変更は禁止。ドリフト検出を有効にする
- IaC リポジトリにも `docs/README.md` を配置し、構成図・変更手順を記載する

### バックアップとリストア
- DB バックアップの設定を文書化する（頻度、保持期間、リストア手順）
- 四半期ごとにリストアテストを実施する（バックアップは「リストアできて初めて意味がある」）
- リストア手順は `docs/disaster-recovery.md` に記載する

## インスタンスサイジングは実績データで判断する

### 「スペックが大きそう」で判断せず、CloudWatch等の実績を見る
- インスタンスクラスのダウンサイズ/アップサイズ、ストレージタイプの変更（io1/gp2→gp3等）を検討する際、インスタンス名やスペック表だけで「過剰/不足」を判断しない
- 最低14日分のCPU使用率・空きメモリ・接続数・IOPS実績を取得し、平均値だけでなく**日次の最大値・スパイクの分布**を確認する
- 単発の瞬間最大値だけで「危険」と判断せず、それが定常的なパターン（バッチ・バックアップ等）か異常かを日次推移で見分ける

### ダウンサイズ時の自動スケーリングパラメータに注意する
- `innodb_buffer_pool_size`や`max_connections`等、RDSがインスタンスメモリに応じて動的計算する設定（`{DBInstanceClassMemory*3/4}`等）は、**固定値に変更せずデフォルト式のまま維持する**
- 固定値を入れると、インスタンスクラス変更のたびに手動再調整が必要になる技術的負債になる。動的計算式のままにしておけば、リサイズ時に自動的に追従する

### ストレージタイプ変更（io1/gp2 → gp3）の判断基準
- 実際のIOPS使用量（Read/WriteIOPS実績）とプロビジョニング量を比較する
- gp3は一定サイズ以下のボリュームで3000 IOPS/125MB/sが追加課金なしで含まれる。プロビジョニング済みIOPSが実績を大きく上回っている場合、gp3への変更でコスト削減とIOPS増加の両方が得られることが多い
- ストレージタイプ変更はオンライン処理（ダウンタイムなし）だが、インスタンスクラス変更は再起動を伴う。ダウンタイムを伴う変更は、他の必須ダウンタイム作業（エンジンバージョンアップグレード等）と同一メンテナンスウィンドウにまとめる

### 発生事例（2026-08-04）
本番RDS（db.m6g.xlarge）のダウンサイズ検討で、CloudWatch実績（CPU平均1-3%、瞬間最大49.74%、IOPS実績が provisioned IOPSの1/3以下）を確認した上でdb.m6g.largeへのダウンサイズを決定。同時に、既存パラメータグループが完全にデフォルトのままだったことを確認し、`innodb_buffer_pool_size`等の動的計算式パラメータには手を入れず、ダウンサイズ後も自動追従する設計を維持した。同じ調査で、ストレージがio1（1500 IOPSプロビジョニング）に対し実使用が平均1桁IOPSだったことも判明し、gp3への変更（無料枠3000 IOPS）も合わせて実施することにした。

### staging検証は「実際に使う値」まで一致させる

パラメータグループ等の設定変更をstagingで事前検証する際、「変更する仕組み自体が動くか」だけでなく、**本番で実際に設定する値そのもの**をstagingでも検証する。仕組みが同じでも、値によってはエンジン側のバリデーション（許容範囲・最小値等）に引っかかることがある。

- stagingでのテストが軽量な値（例: フラグのON/OFFのみ）で、本番では追加のチューニング値（IOPS容量、バッファサイズ等）を設定する場合、その追加値もstagingで一度試しておく
- 特にエンジンのメジャーバージョンが変わる場合、パラメータの許容範囲が旧バージョンと異なることがある（例: あるパラメータの最小許容値が引き上げられる等）。本番作業の当日に初めて発覚すると、その場で値を再検討する手戻りが発生する

#### 発生事例（2026-08-16）
本番RDSのMySQL 8.0→8.4アップグレードに合わせ、新規8.4パラメータグループで`innodb_io_capacity_max=1600`を設定しようとしたところ、`InvalidParameterValue`（MySQL 8.4系の最小許容値は2000）でエラーになった。staging側の事前検証では`slow_query_log`/`long_query_time`のみをテストしており、本番固有のIOPSチューニング値（`innodb_io_capacity`系）はstagingで試していなかったため、本番作業当日に初めて発覚した。その場でストレージの現況（gp3、3000 IOPS baseline）に合わせて3000に変更し事なきを得たが、事前にstagingで同じ値を試していれば当日の手戻りを避けられた。
